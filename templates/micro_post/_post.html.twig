<div id="post-{{post.id}}" class= "mb-4 ">
    <div class="text-2xl">
        <a href="{{ path('app_micro_post_show', { id: post.id }) }}">
        {{ post.title }}
        </a>
    </div>
    <div class="text-xl dark:text-gray-400">
        {{ post.text }}
    </div>
    <div class="text-gray-500 dark:text-gray-400">
        {{ post.created | date("M j, Y") }}
    </div>
    <div>
        {% if post.likedBy.contains(app.user) %}
            <a href="{{ path ('app_unlike', {id:post.id}) }}">
                Unlike <span class="px-2 rounded-md bg-gray-200 dark:bg-gray-700">{{ post.likedBy|length() }}</span>
            </a>
        {% else %}
            <a href="{{ path ('app_like', {id:post.id}) }}">
                Like <span class="px-2 rounded-md bg-gray-200 dark:bg-gray-700">{{ post.likedBy|length() }}</span>
            </a>
        {% endif %}
    </div>
    <div class="text-sm text-gray-500 dark:text-gray-400">
        <a href="{{path('app_profile', {id: post.author.id}) }}">{{post.author.email}}</a>
    </div>
    <div class="flex">
        <a href="{{path('app_micro_post_edit', {id:post.id})}}" class="mr-4">Edit</a>
        <a href="" onclick="del('{{path('app_micro_post_delete', {id:post.id})}}',{{post.id}})" class="mr-4">Delete</a>
        <a href="{{path('app_micro_post_comment', {id:post.id})}}">Comment</a>
        <span class="px-2 rounded-md bg-gray-200 dark:bg-gray-700 ml-2">{{post.comments|length}}</span>        
    </div>               
</div> 